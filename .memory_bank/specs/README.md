# Specifications

This directory contains specifications for the agent skills in this repository. Each skill has its own subdirectory with a `spec.md` file capturing the original requirements and design constraints.

## Structure

```
specs/
└── blockscout-analysis/
    └── spec.md          # Blockscout analysis skill specification
```
